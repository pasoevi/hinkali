"""
A package that contains all the static and dynamic files required to run the web site.
It uses classes stored in the domain package as well as other packages to render views and respond to requests.
"""
